'use client';

import { CrmStubView } from '@/components/admin';

export default function CrmConversionsQueuesPage() {
  return (
    <CrmStubView
      titleKey="admin-pages.crm.sections.conversionsQueues"
      hintKey="admin-pages.crm.sectionHints.conversionsQueues"
      emptyTitleKey="admin-pages.crm.conversionsQueues.emptyTitle"
      emptyHintKey="admin-pages.crm.conversionsQueues.emptyHint"
    />
  );
}
