'use client';

import { CrmStubView } from '@/components/admin';

export default function CrmConversionsMyWorkPage() {
  return (
    <CrmStubView
      titleKey="admin-pages.crm.sections.conversionsMyWork"
      hintKey="admin-pages.crm.sectionHints.conversionsMyWork"
      emptyTitleKey="admin-pages.crm.conversionsMyWork.emptyTitle"
      emptyHintKey="admin-pages.crm.conversionsMyWork.emptyHint"
    />
  );
}
