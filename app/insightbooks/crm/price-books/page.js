'use client';

import { CrmStubView } from '@/components/admin';

export default function CrmPriceBooksPage() {
  return (
    <CrmStubView
      titleKey="admin-pages.crm.sections.priceBooks"
      hintKey="admin-pages.crm.sectionHints.priceBooks"
      emptyTitleKey="admin-pages.crm.priceBooks.emptyTitle"
      emptyHintKey="admin-pages.crm.priceBooks.emptyHint"
    />
  );
}
