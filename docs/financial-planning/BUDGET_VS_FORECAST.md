# Budget versus Forecast

**Budget** — formal management target; versioned; approved budgets immutable; not overwritten by forecasts.

**Forecast** — updated expected outcome; supports baseline, rolling, and scenario versions; never labelled as Budget.

Both use `PlanV2*` tables and never post to the General Ledger.
