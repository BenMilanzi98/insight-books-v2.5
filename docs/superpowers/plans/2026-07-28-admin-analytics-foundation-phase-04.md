# Admin Analytics Foundation Phase 4 — Plan

See programme plan attachment. Waves: 0 docs → 1 schema/lib → 2 producers → 3 consumers/recon → 4 ops UI.

```bash
npx vitest run test/systemAdmin.analytics*.test.js
```
