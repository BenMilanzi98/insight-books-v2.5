# MRA EIS Nav & Tenant Management — Implementation Plan

> **For agentic workers:** implement task-by-task.

**Goal:** Expandable admin MRA EIS nav + persistent section strip; tenant full management nav when subscription OR entitlement unlocks.

## Tasks

1. Shared nav helpers + unlock helper + tests  
2. Admin `adminNav` expandable + layout section strip + path highlight fix  
3. Availability API exposes unlock; Sidebar full/locked menus  
4. Smoke verify  
