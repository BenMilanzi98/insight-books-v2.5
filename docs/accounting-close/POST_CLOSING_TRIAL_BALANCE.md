# Post-Closing Trial Balance

Service: `generatePostClosingTrialBalance` in `postClosingService.js`.

Requires posted Closing Batch. Regenerates TB as-of FY end; validates temporary/drawings zero; requires balanced TB. Status PASSED required before annual snapshots and FY closure.
