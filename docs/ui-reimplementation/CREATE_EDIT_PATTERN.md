# Create / edit pattern

Composition: `components/patterns/CreateEditPage.jsx`

- Narrow `PageContainer` by default.
- Form fields in `Card` with `FormField` + `Input` / `Select` / `Textarea`.
- Associate labels, errors (`aria-invalid`, `aria-describedby`).
- Primary save / cancel in `PageHeader` actions or sticky footer (feature choice preserved).
