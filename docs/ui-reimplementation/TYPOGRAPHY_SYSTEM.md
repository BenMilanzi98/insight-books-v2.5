# Typography System

Font: Geist Sans via `--font-geist-sans` on `body` (fixed from Arial). Mono: Geist Mono for codes. Prefer Tailwind text scale (`text-sm`/`base`/`lg`/`xl`/`2xl`). Financial amounts: `tabular-nums` class when using shared UI.
