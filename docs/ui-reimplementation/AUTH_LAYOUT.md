# Auth / affiliate layout polish

Presentation-only alignment with design tokens:

- Affiliate login / dashboard use `--background-secondary`, `--surface-primary`, `--action-primary`, `--text-*`.
- Tenant login (`/auth/login`) retains its branded marketing composition; no workflow changes.
- Focus: labelled inputs, visible focus rings via `--focus-ring` when migrating controls.
