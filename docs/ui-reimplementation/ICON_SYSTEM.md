# Icon System

Standard: **lucide-react**. Prefer `components/ui` wrappers with `aria-hidden` when decorative; `IconButton` requires `aria-label`. Migrate remaining heroicons/react-icons opportunistically.
