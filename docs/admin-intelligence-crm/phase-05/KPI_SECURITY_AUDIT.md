# KPI Security Audit

Require `systemAdmin.intel.executive.read` or `dashboard.view`. Finance fields need `dashboard.financialMetrics` (or ALLOW_MASKED). Never expose secrets, tenant journals, payroll, credentials.
