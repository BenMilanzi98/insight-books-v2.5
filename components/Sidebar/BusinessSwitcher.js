/**
 * Business (tenant) switcher — re-export of BranchSwitcher under a non-branch name.
 * Branch UI is retired; this control only switches businesses.
 */
export { default } from './BranchSwitcher';
